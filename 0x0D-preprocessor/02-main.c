#include <stdio.h>
/**
  *main - Entry point, prints the name of the file a
  * program was compiled from
  *Return: Always 0 for success
  */

int main(void)
{
printf("%s\n", __FILE__);

return (0);
}
